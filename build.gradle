plugins {
    id 'java'
    id 'org.springframework.boot' version '2.7.9'
    id 'io.spring.dependency-management' version '1.0.15.RELEASE'
}

allprojects {

    repositories {
        mavenCentral()
    }
}

// 부모 프로젝트는 소스코드가 없다.
bootJar {
    enabled = false
}

jar {
    enabled = false
}